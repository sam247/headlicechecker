# YOLO dataset config (Ultralytics). Run from repo root:
#   "$HOME/Library/Python/3.9/bin/yolo" detect train data=public/training/data.yaml model=yolov8n.pt epochs=100

# path is relative to cwd when you run from repo root
path: public/training
train: images/train
val: images/train

nc: 2
names:
  0: lice
  1: nits
