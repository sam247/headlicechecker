# Roboflow (optional, tried first). Use private API key only (never publishable in server env).
# Option A — Legacy model (project/version): set ROBOFLOW_MODEL_ID=project-slug/version (e.g. find-head-lice-psoriases-and-dandruffs/1)
# Option B — Workflow: set ROBOFLOW_WORKSPACE and ROBOFLOW_WORKFLOW_ID from Deploy Workflow in app.roboflow.com
# ROBOFLOW_API_KEY=your_private_api_key
# ROBOFLOW_MODEL_ID=find-head-lice-psoriases-and-dandruffs/1
# ROBOFLOW_WORKSPACE=your-workspace
# ROBOFLOW_WORKFLOW_ID=your-workflow-id

# DeepSeek API (optional): classification + explanation in one call. https://platform.deepseek.com
# DEEPSEEK_API_KEY=sk-...
# DEEPSEEK_API_BASE=https://api.deepseek.com

# Optional: your own detection API. POST /predict with { image: base64 }, returns { label, confidence?, explanation? }.
# DETECTION_API_URL=https://your-model.example.com

# If none are set, /api/scan returns a random stub. Images are never stored (in-memory only).
